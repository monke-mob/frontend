<div id="contact" class="relative scroll-margin-top"></div>

<div class="w-screen py-[20vh] brandGradient min-h-[50vh] safe-area-padding">
    <p class="brandTitle">contact us</p>
</div>

<style>
    .scroll-margin-top {
        scroll-margin-top: 60px; /* Adjust this value to match the height of your header */
    }

    /* Safe area padding */
    .safe-area-padding {
        padding-top: env(safe-area-inset-top, 20px);
        padding-bottom: env(safe-area-inset-bottom, 20px);
    }

    .brandGradient {
        min-height: calc(10vh + env(safe-area-inset-bottom, 0px));
    }
</style>
