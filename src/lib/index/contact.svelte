<div id="contact" class="relative"></div>

<div class="w-screen py-[20vh] brandGradient min-h-[50vh] safe-area-padding">
    <p class="brandTitle">contact us</p>
</div>

<style>
  .safe-area-padding {
    padding-bottom: env(safe-area-inset-bottom);
    padding-top: env(safe-area-inset-top);
  }

  .brandGradient {
    min-height: calc(10vh + env(safe-area-inset-bottom));
  }
</style>
